steps to start meteor project cloned from git:

1. clone project from git into the same drive as mongo as meteor
2. open cmd in the project folder
3. run "meteor npm install"
4. run "meteor"

meteor server should be running now on 'localhost:3000'